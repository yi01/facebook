<p><%= notice %></p>

<h1>ブログ一覧</h1>
<ul>
  <% @blogs.each do |blog| %>
    <p><%= link_to "このブログを見る", blog_path(blog.id) %></p>
    <li>タイトル：<%= blog.title %></li>
    <li>内容：<%= blog.content %></li>
    <%= link_to "ブログを編集する", edit_blog_path(blog.id) %>
    <%= link_to "ブログを削除する", blog_path(blog.id), method: :delete, data: {confirm: "are you sure?"} %>
  <% end %>
</ul>
<p>
<%= link_to 'ブログを作成する', new_blog_path %>
</p>

<p>
  <%= link_to 'お問い合わせ', new_contact_path %>
</p>
